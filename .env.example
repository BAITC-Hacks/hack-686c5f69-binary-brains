# Server only. Copy to .env.local and fill locally. Never use NEXT_PUBLIC_ names.
# Catalog service (participant 1).
EKT_API_USERNAME=
EKT_API_PASSWORD=
# Assistant's current client (participant 2): use the same login for EKT_API_USER.
EKT_API_BASE_URL=https://ekt.kz/api
EKT_API_USER=
EKT_API_SEARCH_PAGES=3
